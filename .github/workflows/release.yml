name: release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-and-release:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew update
          brew install cmake pkg-config

      - name: Configure
        if: runner.os != 'Windows'
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: |
          cmake --build build --config Release --parallel -DCMAKE_POLICY_VERSION_MINIMUM=3.5

      - name: Install (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          cmake --install build --prefix dist

      - name: Install (Windows)
        if: runner.os == 'Windows'
        run: |
          cmake --install build --prefix dist --config Release

      - name: Package (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          OS_NAME=$(echo ${{ runner.os }} | tr '[:upper:]' '[:lower:]')
          tar czf mario-kart-${{ github.ref_name }}-${OS_NAME}-x64.tar.gz -C dist .

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Compress-Archive -Path dist\* -DestinationPath mario-kart-${{ github.ref_name }}-windows-x64.zip

      - name: Upload Release Asset
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            mario-kart-${{ github.ref_name }}-linux-x64.tar.gz
            mario-kart-${{ github.ref_name }}-macos-x64.tar.gz
            mario-kart-${{ github.ref_name }}-windows-x64.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
